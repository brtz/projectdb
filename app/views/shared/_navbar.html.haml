%nav.navbar.navbar-dark.sticky-top.bg-dark.flex-md-nowrap.p-0
  = link_to 'ProjectDB', root_path, class: 'navbar-brand col-sm-3 col-md-2 mr-0'
  %ul.navbar-nav.px-3.full-width
    - if user_signed_in?
      %li.nav-item.text-nowrap
        = link_to "#{current_user.first_name} #{current_user.last_name} (#{current_user.email}) Abmelden", destroy_user_session_path, method: :delete, class: 'nav-link float-right'
